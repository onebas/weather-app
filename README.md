# A simple Weather website

It accesses the Visual Crossing API to get the current temperature of the specified city using asynchronous code. 
It automatically alerts user if incorrect input is given, Uses features like Promises & async/await for implenting the requests.